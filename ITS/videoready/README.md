# videoready

This directory is a placeholder for the local raw-video archive.

The actual raw videos are kept on the workstation under:

```text
D:\a_fpga\codex\5_12_HDMI_IN_DDR3_HDMI_OUT\else\ITS\videoready
```

They are not committed to GitHub because several files exceed GitHub's normal
100 MB file limit. The smaller current test videos used by the demo are kept in
`ITS/video/`.
